import { createBottomTabNavigator } from "@react-navigation/bottom-tabs";

const TabNavigators = () => {
  const Tab = createBottomTabNavigator();

  return (
    <Tab.Navigator>
      {/* <Tab.Screen name="Ingressos" component={}/> */}
    </Tab.Navigator>
  );
};
export default TabNavigators;
